(alteração teste)

git init
git config --global user.name"<UsuarioGit>"
git config --global user.email"<e-mail>"
git remote add origin <https.git>
git status

git add . //Para adicionar todos os arquivos no repositorio local
git commit -m "<Explicação das alterações>"
git push -u origin master //-u Só no primeiro push

